# TamanduOffice

Administrative automations.

## Getting Started

```bash
pnpm install
```

- Create an `input` folder in the `src` folder and put the files you want to process there.
- Create an `output` folder in the `src` folder. This is where the processed files will be saved.

Run the project locally:

```bash
pnpm run dev
```
